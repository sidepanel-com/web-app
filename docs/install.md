
Initialize the next repo
```
npx create-next-app@latest begin-boring2 --typescript --tailwind --biome --pages --src-dir 
```
initialize the supabase repo
```
npx supabase init
```
start the supabase server and get the connection string
```
npx supabase start
```


```
npm install @supabase/supabase-js
npm install @supabase/ssr
```
```
npx shadcn@latest init 
npx shadcn@latest add --all
```

